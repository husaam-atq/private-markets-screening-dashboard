# Retrieval Evaluation

The gold set contains deterministic diligence-style questions against cached sample filing chunks.
Relevance is measured through expected section labels and expected keyword matches. No-answer
questions are used to check that weak evidence is flagged instead of converted into unsupported
claims.
