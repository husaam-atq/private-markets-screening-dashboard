[project]
name = "private-markets-screening-dashboard"
version = "0.1.0"
description = "Public-data private markets screening and SEC filing intelligence dashboard."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"

[tool.ruff]
line-length = 100
